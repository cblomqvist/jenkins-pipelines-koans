rootProject.name = 'jenkins-pipelines-koans'

sourceControl {
    gitRepository("https://github.com/swedbank/jenkins-stargate.git") {
        producesModule("com.swedbank:jenkins-pipeline-test")
    }
}